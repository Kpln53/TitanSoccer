- If a tool call fails, do not repeat the same tool call; instead, summarize the error, propose the next concrete step (e.g., inspect logs/compile errors), and proceed only after fixing the underlying issue.